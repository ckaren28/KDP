[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."museum-label"]
  timeout = 26

[functions."garment-decoder"]
  timeout = 30

[functions."textile-palette"]
  timeout = 20
